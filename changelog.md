# changeglob

## 1.1

- Export the bin as `glob`, which is what it should've been all
  along.

## 1

- Split the glob CLI out from the main `node-glob` project into
  this one so that it can be maintained separately.
